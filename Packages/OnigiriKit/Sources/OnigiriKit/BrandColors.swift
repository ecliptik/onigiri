import SwiftUI

public extension Color {
    /// The warm cream from the app icon's background — bright accent for
    /// filled controls (pair with dark text).
    static let ricePaper = Color(red: 0.96, green: 0.83, blue: 0.62)

    /// A deeper toasted tan with enough contrast to serve as the app-wide
    /// tint (tab selection, links, pickers).
    static let riceToast = Color(red: 0.72, green: 0.51, blue: 0.25)
}
